# ─────────────────────────────────────────────────────────────
# services/inference/requirements.txt
# Versions pinned — obligatoire pour la reproductibilité
#
# Ce service charge les 2 modèles sklearn/xgboost
# et appelle les autres services via httpx (client HTTP async)
# ─────────────────────────────────────────────────────────────

fastapi==0.111.0        # framework API REST
uvicorn==0.29.0         # serveur ASGI
pydantic==2.7.1         # validation des données d'entrée
httpx==0.27.0           # client HTTP async pour appeler preprocessing et monitoring
joblib==1.4.2           # chargement des fichiers .pkl
scikit-learn==1.5.1     # Pipeline + preprocessing (ColumnTransformer, etc.)
xgboost==2.0.3          # modèle offer_pipeline (XGBoost multi-classe)
pandas==2.2.2           # conversion dict → DataFrame pour les Pipeline sklearn
numpy==1.26.4           # dépendance de sklearn et xgboost